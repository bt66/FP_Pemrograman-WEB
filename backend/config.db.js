const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "fiberku.site",
      user: "root",
      password: "P3mrogW3B",
      database: "FP_WEB",
    }
  };
  module.exports = config;